using UnityEngine;

public class RoomSelector : MonoBehaviour
{
    private const int _roomDistance = 5;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }


}
